# Лабораторная работа №2  
## Использование соединений (JOIN), подзапросов и функций преобразования данных

**Номер варианта:** 13  

---
# Цель работы

Освоить методы объединения таблиц (JOIN, UNION), работу с подзапросами и функции преобразования данных (CASE, COALESCE) в PostgreSQL.

# Задания

## Задание 1. Использование JOIN  
Вывести сумму продажи и базовую цену товара для всех транзакций 2019 года.

Пояснение логики: Для получения информации о продажах и ценах товаров используется INNER JOIN таблиц sales и products по product_id. Фильтрация по 2019 году выполнена с помощью функции EXTRACT.

Скриншот выполнения SELECT (JOIN, основная БД):
[задание 2.1](https://github.com/MakhoninaAV/sql_files/blob/main/02_01.jpg)



---

## Задание 2. Использование подзапроса 
Найти клиентов, купивших товары типа 'automobile' (через подзапрос в sales).

Пояснение логики: Подзапрос находит все customer_id из таблицы sales, которые покупали товары типа 'automobile' (через соединение с products). Внешний запрос возвращает полную информацию о таких клиентах. DISTINCT убирает дубликаты, так как один клиент мог купить несколько автомобилей.

Скриншот выполнения  SELECT (подзапрос, основная БД):
[задание 2.2](https://github.com/MakhoninaAV/sql_files/blob/main/02_02.jpg)


---

## Задание 3. Преобразование данных (COALESCE)  
Если у клиента ip_address NULL, вывести 'Unknown IP', иначе сам IP.

Пояснение логики: Функция COALESCE проверяет поле ip_address и возвращает его значение, если оно не NULL. Если значение отсутствует (NULL), подставляется строка 'Unknown IP'.

Скриншот выполнения SELECT (COALESCE, основная БД):
[задание 2.3](https://github.com/MakhoninaAV/sql_files/blob/main/02_03.jpg)


---

# Вывод

В ходе лабораторной работы были:

- выполнены запросы с использованием `JOIN` для объединения таблиц sales и products с фильтрацией по дате;
- применён подзапрос с `IN` для нахождения клиентов, покупавших товары определённого типа;
- ииспользована функция `COALESCE` для замены NULL-значений на строку по умолчанию.

Все задания выполнены в соответствии с вариантом.  
Файл [lab_02 махонина.sql](https://github.com/MakhoninaAV/sql_lab/blob/main/lab_02/lab_02%20%D0%BC%D0%B0%D1%85%D0%BE%D0%BD%D0%B8%D0%BD%D0%B0.sql) содержит чистый код выполненных запросов.
